# Learnings — clone-triple (append-only)

The ONLY file under `formats/clone-triple/` agents may write. Append dated entries when
a run teaches something; never edit the recipes/SOP directly. Entries contradicting a
LOCKED rule are logged but NOT applied. Promotion into recipes is a human act.

Format: `- YYYY-MM-DD: <what was learned> → <how to apply it>`

- 2026-07-08: (seed) clone-triple is `feasible`, not `proven` — the non-speaking clone
  is the drift risk. If triple keeps drifting, fall back to clone-double and log the
  scene here.
