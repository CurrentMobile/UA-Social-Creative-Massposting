# Prompts — clone-triple B-roll (`gpt_image_2` → `kling3_0`, sound OFF)

One cutaway per beat (quota). Brainstorm fresh from
`assets/_shared/b-roll-bank/bank.md`, grep the registry, append winners. Muted;
duration matches the covered turn.

## Mechanics
- **App reveal (the hyped clone's proof):** phone reveal or activity insert showing the
  REAL app UI per the phone visibility grammar (`s22-ultra-front.png` +
  `assets/<app>/brand/<ui>.png`). Usually cut in when the hyped clone says "show me" /
  the payoff.
- **Metaphor / reaction gag:** a single-subject cutaway visualizing the argument (e.g.
  a scale tipping, a "scam alarm" that fizzles out) — no clones needed, keeps it cheap.

## Worked shape (app reveal)
> Tight over-the-shoulder of a hand holding a Black Samsung Galaxy S22 Ultra, its
> bright screen facing the camera clearly showing the Mode Earn App reward confirmation
> exactly as in the reference image, sharp and legible, warm bokeh behind. Slight
> natural handheld only.
