# Prompt — reaction clip (`kling3_0`, sound ON)

Start frame = the reactor setup still. Big expressive commentary; voice tag verbatim;
camera static; continuity close.

## Template
> Begin on the `<green-screen medium shot / corner framing>` of `<persona>` from the
> reference, reacting with `<attitude: skeptical → convinced / amused / hot-take
> energy>` in `<voice tag verbatim>`: "`<turn dialogue, exact words>`". `<Big readable
> reaction: eyes widening, a laugh, pointing at the footage, a reluctant nod>`.
> Wardrobe, lighting, and background completely consistent, camera static — a seamless
> single take.

## Worked example
> Begin on the green-screen medium shot of the reactor from the reference against the
> neutral background, reacting with animated skepticism sliding into grudging respect in
> `<voice tag verbatim>`: "I hate that it actually works. Fine. It's legit." A dramatic
> eye-roll, hands up in mock surrender, then a small nod. Wardrobe, lighting, and
> background completely consistent, camera static — a seamless single take.
