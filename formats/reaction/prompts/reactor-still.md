# Prompt — reaction reactor setup still (`gpt_image_2`)

Ref: persona `base-character.png`. Cross-format rules apply. Inject guardrails first.

## green-screen-style (keyable background)
> A medium head-and-shoulders shot of `<persona>` from the reference facing the camera,
> expressive and animated, against a plain evenly-lit `<solid neutral / soft gradient>`
> background suitable for background removal, `<casual wardrobe>`, natural key light, no
> props or busy background. Photorealistic. Camera completely static.

## corner-cam (normal setup framed for a bubble)
> A medium shot of `<persona>` from the reference in `<a simple room>`, framed
> head-and-shoulders with margin so it crops cleanly into a corner bubble, animated
> expression, `<casual wardrobe>`, soft light. Naturally grounded, photorealistic.

Keep the reactor's expression lively — this format lives on big, readable reactions.
