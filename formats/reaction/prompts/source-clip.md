# Reacted-to source clip — reaction

<!-- LOCKED --> Owned or generated content ONLY. Prefer our own app footage or a prior
ad from `assets/<app>/`. Generate an app-UI "claim" clip only when nothing owned fits.
Never react to a third party's copyrighted video without the owner's explicit clearance.
<!-- /LOCKED -->

## Generated "claim" clip (`gpt_image_2` still → `kling3_0`, sound OFF)
An app-UI demo the reactor fact-checks; screen to viewer, real UI (front prop + app-UI
refs):
> A clean close-up of a Black Samsung Galaxy S22 Ultra screen (screen-to-viewer)
> walking through the <app> `<earning grid → cashout → $25 gift card>` exactly as in
> the reference UI, a finger tapping through; clear, legible, satisfying, static camera.

Muted; the reactor narrates over it.
