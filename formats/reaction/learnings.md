# Learnings — reaction (append-only)

The ONLY file under `formats/reaction/` agents may write. Append dated entries when a
run teaches something; never edit the recipes/SOP directly. Entries contradicting a
LOCKED rule are logged but NOT applied. Promotion into recipes is a human act.

Format: `- YYYY-MM-DD: <what was learned> → <how to apply it>`

- 2026-07-08: (seed) `feasible` — gated on source-clip policy (owned/generated only)
  and, for green-screen-style, a clean reactor matte. Record matte-background choices
  that key cleanly here.
