# Prompt — split-screen A-roll clip (`kling3_0`, sound ON)

Identical to ugc-single (`formats/ugc-single/prompts/a-roll-clip.md`) — pose-match the
crop-safe extract, voice tag verbatim, exact dialogue, camera static/drift, continuity
close. The only added constraint: keep the key action centered so the lane crop keeps
it in frame. Phone (if held) follows the phone visibility grammar.

See the ugc-single worked example; apply it with the crop-safe framing note.
