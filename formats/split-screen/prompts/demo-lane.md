# Demo lane — split-screen

<!-- LOCKED --> PREFER a real app screen recording from
`assets/_shared/screen-recordings/<app>/` (listed in the app manifest) — it's the real
UI and free. Only generate when no recording matches the beat. <!-- /LOCKED -->

## Sourcing (preferred)
Pick the recording that matches the beat: earning/home grid at HOW IT WORKS, cashout at
RESULT, activity (music/games/news) at SOLUTION. Trim/loop to the beat's span; muted.

## Generated fallback (`gpt_image_2` still → `kling3_0`, sound OFF)
Only if no recording exists. A "satisfying"/ambient loop that reads as the app in use;
if a phone/screen is shown, phone visibility grammar (real UI, front prop + app-UI ref).
> A clean close-up of a Black Samsung Galaxy S22 Ultra screen (screen-to-viewer)
> showing the <app> `<earning grid / cashout / activity>` UI exactly as in the
> reference, a finger gently scrolling; calm satisfying motion that loops seamlessly,
> static camera.

Whatever the source, the demo lane always shows the REAL app UI.
