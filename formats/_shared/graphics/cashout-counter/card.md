# Card: cashout-counter

**What:** the real cashout-balance screenshots switching in ASCENDING order
(3K→24K→100K→redeem) — the growing real balance IS the count-up animation.
**Fires at:** the payoff beat ("points add up… redeem").
**Assets:** `assets/<app>/brand/<cashout screenshots dir>/` in ascending balance order.
**Build:** alpha-WebM card; one switch per screenshot, **one shutter SFX per switch**.
**EDL:** `overlays` entry + shutter SFX times in `mix.json`.
