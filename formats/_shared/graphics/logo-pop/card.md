# Card: logo-pop

**What:** the app's animated logo pop-up overlay.
**Fires at:** synced EXACTLY to the spoken app name whenever it's introduced
("I found Mode Earn App") — read the time from `master.srt`.
**Assets:** the app's alpha logo animation (MEA:
`brand/MEA-Logo-Animation-POP-UP.webm`, alpha, `keep_audio:false`). Use the WebM —
browsers can't decode ProRes MOV in the editable preview.
**EDL:** `overlays` entry, overlaid on BOTH A-roll and B-roll.
