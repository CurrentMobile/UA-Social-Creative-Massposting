# Learnings — play-pause (append-only)

The ONLY file under `formats/play-pause/` agents may write. Append dated entries when a
run teaches something; never edit the recipes/SOP directly. Entries contradicting a
LOCKED rule are logged but NOT applied. Promotion into recipes is a human act.

Format: `- YYYY-MM-DD: <what was learned> → <how to apply it>`

- 2026-07-08: (seed) `feasible` — asset-simple, edit-clever. The PiP freeze + pause-UI
  + tape-stop SFX sync is the make-or-break; record timing tricks here after the first run.
