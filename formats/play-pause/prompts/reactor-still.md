# Prompts — play-pause watching-setup still (`gpt_image_2`)

The reactor at their device, angled front view, with clean top-corner space for the
PiP. Cross-format rules apply. Inject guardrails first.

## environment {#environment}
> `<A desk / couch setup: a home desk with a laptop, a cozy couch with a phone>`, warm
> soft lighting, tidy, no people. Photorealistic.

## watching still (refs: base-character + environment)
> A slightly-angled front-view medium shot of `<persona>` from the reference sitting at
> `<the setup>` in `<environment>`, facing the camera at a natural three-quarter angle,
> `<their device>` in front of them. Relaxed, curious expression, `<casual wardrobe>`,
> soft key light, a clean uncluttered upper-`<left/right>` corner area for a
> picture-in-picture overlay. Naturally seated, feet grounded, photorealistic. The
> camera is completely static.

Keep the corner genuinely empty (no lamp/plant there) so the PiP composites cleanly.
