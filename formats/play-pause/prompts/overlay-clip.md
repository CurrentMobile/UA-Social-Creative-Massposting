# Overlay (PiP) clip — play-pause

<!-- LOCKED --> The overlaid content shows the REAL app UI. PREFER a real screen
recording from `assets/_shared/screen-recordings/<app>/` (listed in the app manifest).
Generate only on miss. Imported ONCE and reused across the PiP windows. <!-- /LOCKED -->

## Sourcing (preferred)
Pick recording moments that set up each pause: earning grid with points ticking (→
"rewarding her for playing music?"), cashout with a gift card (→ "that's a real $25").
Muted; the reactor narrates.

## Generated fallback (`gpt_image_2` still → `kling3_0`, sound OFF)
> A close-up of a Black Samsung Galaxy S22 Ultra screen (screen-to-viewer) showing the
> <app> `<earning grid with points incrementing / cashout with a $25 gift card>` UI
> exactly as in the reference, a finger interacting; clear, legible, static camera.
(Refs: `s22-ultra-front.png` + `assets/<app>/brand/<ui>.png`.)

The PiP is what "pauses" — pick moments with a clear beat to freeze on.
