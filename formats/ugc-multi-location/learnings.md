# Learnings — ugc-multi-location (append-only)

The ONLY file under `formats/ugc-multi-location/` agents may write. Append dated entries
when a run teaches something; never edit the recipes/SOP directly. Entries contradicting
a LOCKED rule are logged but NOT applied. Promotion into recipes is a human act.

Format: `- YYYY-MM-DD: <what was learned> → <how to apply it>`

- 2026-07-08: (seed) `feasible` — cross-location identity + wardrobe drift is the risk.
  One-outfit policy is the safe default; per-location outfits are higher production but
  need tighter QA. If drift is bad, drop to 3 locations.
