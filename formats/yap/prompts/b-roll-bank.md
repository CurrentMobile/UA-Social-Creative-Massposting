# Prompts — yap B-roll (minimal)

<!-- LOCKED --> B-roll is minimal-to-none — the raw talking IS the format. At most 1-2
quick cutaways for the app reveal at the HOW/RESULT beat. Do NOT apply the ugc B-roll
quota. <!-- /LOCKED -->

## The one cutaway that earns its place: the app reveal
Phone up showing the real app UI, per the phone visibility grammar (screen to viewer,
`s22-ultra-front.png` + `assets/<app>/brand/<ui>.png` refs):
> A quick close selfie-angle insert of `<persona>` flipping their Black Samsung Galaxy
> S22 Ultra toward the camera, its bright screen clearly showing the <app> `<earning
> grid / cashout>` exactly as in the reference, "look!" energy. Slight handheld only.

Muted, short (≤ its beat). Anything beyond this is usually too much for yap — let the
captions and the face carry it. Log any concept you do ship in the bank registry.
