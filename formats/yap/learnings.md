# Learnings — yap (append-only)

The ONLY file under `formats/yap/` agents may write. Append dated entries when a run
teaches something; never edit the recipes/SOP directly. Entries contradicting a LOCKED
rule are logged but NOT applied. Promotion into recipes is a human act.

Format: `- YYYY-MM-DD: <what was learned> → <how to apply it>`

_(no entries yet — first real run will seed this)_
